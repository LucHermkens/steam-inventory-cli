{
    "name": "steam-inventory-cli",
    "version": "1.0.0",
    "packageManager": "bun@1.3.2",
    "scripts": {
        "dev": "bun run --watch src/index.ts",
        "build:windows": "bun build --compile --target=bun-windows-x64 src/index.ts --outfile dist/steam-inventory-cli.exe",
        "build:linux": "bun build --compile --target=bun-linux-x64 src/index.ts --outfile dist/steam-inventory-cli",
        "build": "bun run build:windows && bun run build:linux",
        "start": "bun run src/index.ts",
        "lint": "biome check",
        "lint:fix": "biome check --write",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@biomejs/biome": "2.3.6",
        "@types/bun": "1.3.2",
        "typescript": "^5.9.3"
    }
}
